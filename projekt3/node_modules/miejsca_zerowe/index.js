//class MZ {
    
    function obliczMiejsceZerowe(a,b,c)
    {
        var str;

        if (a == 0)
        {
         str = "To nie jest rownanie kwadratowe";
        }
        else
        {
         var delta = b*b - 4*a*c;
         var p = Math.sqrt(delta);
        
         if (delta == 0)
         {
          var wynik = -b / (2*a);
          str = "Rownanie ma jedno rozwiazanie, wynosi: " + wynik;
         }
        
         if (delta > 0)
         {
          var x1 = (-b - p) / (2*a);
          var x2 = (-b + p) / (2*a);
          str = "Rownanie ma dwa rozwiazania, pierwsze wynosi: " + x1 + ", a drugie: " + x2;
         }
         else
         {
          str = "Delta jest ujemna, rownanie nie ma rozwiazan!";
         } 
         return str;
    }
    }

    //};
    
    //module.exports = { MZ: MZ};
    exports.oblicz=obliczMiejsceZerowe;